# 战国谋士 v7.3 — V2 艺术资产清单

## 视口
1152 × 648（沿用）

## 已存在资产（沿用，不重新生成）
- `res://assets/bg/main_bg.png` — 主场景背景
- `res://assets/fonts/NotoSansSC-Regular.ttf` — 中文字体
- `res://default_theme.tres` — 默认主题

## Phase A 不新增资产
Phase A 只做数据层与仲裁层，沿用现有资产。UI 美术资产清单在 Phase C 设计文档中追加。

## Phase B / C 待规划
- 卡牌底图 ×7（每种动作各一）
- 君主立绘 ×3（秦/赵/齐）
- 国家场景背景 ×3
- MBTI 结算页背景
- 按钮、面板状态贴图（写入 default_theme.tres）
- BGM ×1 + SFX 抽牌 / 打牌 / 采纳 / 拒绝 / 即死
